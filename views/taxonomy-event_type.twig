{% extends "base.twig" %}

{% block content %}
	<pre>this is taxonomy-event_type.twig</pre>
	{% set term = TimberTerm(term) %}
	<pre>{{dump(TimberImage(term.event_type_image).src)}}</pre>
	{{term.name}}
	{% for post in posts %}
		{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
	{% endfor %}
{% endblock %}